package telran.stream;

public class StreamTasks {

    public static int[] shuffle(int[] arr) {
        // only one pipeline
        // without sorting
        // Arrays.stream()..
        //return one array of ints with all numbers from a given array but with different order
        //each method call 
        return null;
    }

}
